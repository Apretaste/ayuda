<div class="row">
	<div class="col s12">
		<input type="hidden" value="<%= parentTicket %>" id="parent">
		<ul class="collection one-line">
			<li class="collection-item">
				<span class="title"><%= ticket.body %></span>
				<p class="blue-grey-text"><%= moment(ticket.creation_date).format('MMM D, YYYY h:mm A')  %></p>
			</li>
		</ul>
	</div>
</div>

<!-- LIST OF CHATS -->
<div class="row">
	<div class="col s12 bubble">
		<ul class="chat">
			<% chat.forEach(function(item) { %>
				<li class="<%= item.position %>">
					<div class="person-avatar circle" face="<%= item.avatar %>" color="<%= item.color %>" size="30"></div>
					<div class="head">
						<a href="#!" class="<%= item.gender %>">@<%= item.username %></a>
						<span class="date"><%= moment(item.date).format('MMM D, YYYY h:mm A') %></span>
					</div>
					<span class="text"><%= item.text %></span>
				</li>
			<% }) %>
		</ul>
	</div>

	<!-- NO CHAT MESSAGE -->
	<div id="msg" class="row hidden">
		<div class="col s12 center-align grey-text">
			<p>Escríbanos con dudas o sugerencias. Respondemos en menos de un día, pero en casos excepcionales puede demorarnos hasta 72 horas. También puede escribir a <%= support %></p>
		</div>
	</div>

</div>

<div class="bottom-space"></div>

<!-- FLOATING BUTTON -->
<div class="fixed-action-btn">
	<a class="btn-floating waves-effect waves-light grey second" href="#!" onclick="apretaste.send({command: 'AYUDA SOPORTE'})">
		<i class="material-icons">arrow_back</i>
	</a>
	<a href="#newTicketModal" class="btn-floating btn-large waves-effect waves-light modal-trigger">
		<i class="large material-icons">message</i>
	</a>
</div>

<!-- MODAL NEW TICKET -->
<div id="newTicketModal" class="modal bottom-sheet">
	<div class="modal-content">
		<div class="input-field">
			<textarea id="message" class="materialize-textarea" oninput="messageLengthValidate()"></textarea>
			<label for="message">Escriba su comentario</label>
			<span class="helper-text">Restante: 1000</span>
		</div>
	</div>
	<div class="modal-footer">
		<a href="#!" class="modal-close waves-effect waves-light btn-flat">Cerrar</a>
		<a onclick="sendMessage(2)" class="modal-close waves-effect waves-green btn-flat">Enviar</a>
	</div>
</div>