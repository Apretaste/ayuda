<div class="row">
    <div class="col s12">
        <ul class="tabs tabs-fixed-width">
            <li class="tab"><a href="#" onclick="apretaste.send({command: 'AYUDA FAQ'})">Preguntas</a></li>
            <li class="tab"><a href="#" class="active">Soporte</a></li>
        </ul>
    </div>
</div>

<input type="hidden" value="" id="parent">

<div class="row">
    <div class="col s12">
        <p class="blue-grey-text">Estos son tus tickets o dudas. Puedes agregar uno nuevo y por cada uno seleccionarlo para establecer una conversaci&oacute;n con el soporte, donde recibir&aacute;s las respuestas.</p>
        <ul class="collection one-line" style="border: none;">
            <% tickets.forEach(function(item) { %>
                <a href="#!" onclick="apretaste.send({command: 'AYUDA CHAT', data:{parent: <%= item.id %>}});">
                <li class="collection-item avatar">
                    <i class="tiny material-icons circle fa fa-comment"></i>
                    <span class="badge comments"><%= item.comments %></span>
                    <span class="title"><%= item.text %></span>
                    <p class="blue-grey-text"><%= moment(item.date).format('MMM D, YYYY h:mm A')  %></p>
                </li>
                </a>
            <% }) %>
        </ul>
    </div>

    <div class="col s12">
        <p class="blue-grey-text center">Escríbanos con dudas o sugerencias. Respondemos en menos de una hora pero en casos excepcionales puede tomarnos hasta 72 horas. También puede escribir a <%= support %></p>
    </div>
</div>

<div class="bottom-space"></div>

<!-- FLOATING BUTTON -->
<div class="fixed-action-btn">
    <a href="#newTicketModal" class="btn-floating btn-large waves-effect waves-light modal-trigger">
        <i class="large material-icons">add</i>
    </a>
</div>

<!-- MODAL NEW TICKET -->
<div id="newTicketModal" class="modal bottom-sheet">
    <div class="modal-content">
        <div class="input-field">
            <textarea id="message" class="materialize-textarea" oninput="messageLengthValidate()"></textarea>
            <label for="message">Escriba su duda o inconveniente</label>
            <span class="helper-text">Restante: 1000</span>
        </div>
    </div>
    <div class="modal-footer">
        <a href="#!" class="modal-close waves-effect waves-light btn-flat">Cerrar</a>
        <a onclick="sendMessage()" class="modal-close waves-effect waves-green btn-flat">Enviar</a>
    </div>
</div>